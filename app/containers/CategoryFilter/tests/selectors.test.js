// import { fromJS } from 'immutable';
// import { makeSelectCategoryFilterDomain } from '../selectors';

// const selector = makeSelectCategoryFilterDomain();

describe('makeSelectCategoryFilterDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
